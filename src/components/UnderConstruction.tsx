import React from 'react';

// Test-only branch: `main` keeps the public site gate intact.
export default function UnderConstruction({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
